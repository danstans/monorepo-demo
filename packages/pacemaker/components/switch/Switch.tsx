import { SwitchProps } from "./Switch.types";
import Switch from "@mui/material/Switch";

const CapableSwitch = (props: SwitchProps): JSX.Element => {
  return <Switch />;
};

export default CapableSwitch;
