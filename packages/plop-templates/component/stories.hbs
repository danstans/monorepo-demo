import {{name}} from "../components/{{dashCase name}}//{{name}}";
import { {{name}}Props } from "../components/{{dashCase name}}/{{name}}.types";
import { Meta, Story } from "@storybook/react";

export default {
  title: "Shared/{{name}}",
  component: {{name}},
} as Meta;

const Template = (args: {{name}}Props) => <{{name}} {...args} />;

export const InitialStory: Story<{{name}}Props> = Template.bind({});

InitialStory.args = {};